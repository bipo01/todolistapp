<style>
  @media screen and (max-width: 1000px) {
    .tarefas-detalhes {
      grid-row: 2/21;
      grid-column: 1/21;
      flex-direction: column;
      padding: 5px;
      align-items: center;
      overflow-y: auto;
      flex-direction: row;
    }

    .tarefas-state {
      width: 100% !important;
      max-width: 100% !important;
      min-height: 80% !important;
      gap: 5px;
      overflow-y: auto;
    }

    .tarefas {
      overflow-y: auto;
      padding: 5px;
    }

    .showNewTask-addSelectUser {
      grid-column: 1/21 !important;
      grid-row: 1/2 !important;
      display: flex;
      justify-content: space-between;
    }

    #newTask {
      height: 100%;
      font-size: 12px;
    }

    .newTask-selectUser {
      height: 100%;
      display: flex;

      justify-content: center;
    }

    .addSelectUser {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }

    .addSelectUser > * {
      width: 100%;
    }

    #selectUserTasks {
      padding: 0.5rem;
      border-radius: 5px;
      cursor: pointer;
    }

    #minhasListas {
      font-size: 0; /* Esconde o texto */
    }
    .minhasListas-novaListas {
      position: fixed;
      bottom: 1%;
      left: 0;
      gap: 0;
      display: flex;
      flex-direction: column; /* <-- aqui */
      align-items: center;
      justify-content: center;
    }

    .minhasListas-novaListas a {
      margin: 2px !important; /* Garante que nenhuma margem atrapalhe */
      padding: 10px !important;
    }

    #minhasListas::before {
      content: "\f015"; /* código do ícone "fa-house" */
      font-family: "Font Awesome 6 Free";
      font-weight: 900;
      font-size: 16px; /* Mostra o ícone */
      border-radius: 5px;
      margin: 0;
    }

    #newSheet {
      font-size: 0; /* Esconde o texto */
    }

    #newSheet::before {
      content: "\f055"; /* código do ícone "fa-house" */
      font-family: "Font Awesome 6 Free";
      font-weight: 900;
      font-size: 16px; /* Mostra o ícone */
      border-radius: 5px;
      margin: 0;
    }

    #selectCategoryMedia {
      display: block;
      height: 100%;
    }

    .h1-btns {
      display: none;
    }

    form[action="/add"],
    form[action="/edit"] {
      background: #3a3a3b;
      margin: 5px;
      display: flex;
      gap: 15px;
      align-items: center;
      justify-content: center;
      padding: 1rem;
      border-radius: 10px;
      flex-direction: column;
      width: fit-content;
      min-width: 75%;
      min-height: 75%;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
    }

    form[action="/add"] input,
    form[action="/add"] button,
    form[action="/add"] select,
    form[action="/edit"] input,
    form[action="/edit"] button,
    form[action="/edit"] select {
      font-size: 20px;
      height: 50px;
    }

    .dificuldade-prazo {
      flex-wrap: wrap;
    }
  }

  @media screen and (max-width: 800px) {
    .tarefas-detalhes {
      flex-direction: column !important;
    }
  }
</style>

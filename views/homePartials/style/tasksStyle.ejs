<style>
  .tarefas-detalhes {
    width: 100%;
    border-radius: 10px;
    background: white;
    display: flex;
    flex-direction: row;
    gap: 10px;
    padding: 1.5rem;
    height: 100%;
    align-items: center;
    justify-content: space-between;
    margin: 0;
  }

  .tarefas-state {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    gap: 20px;
    width: 33.33%; /* Cada um ocupa exatamente 1/3 do container */
    height: 100%;
    max-height: 100%;
    max-width: 33.33%;
    flex: 1; /* Garante que todos tenham o mesmo tamanho */
    min-width: 0; /* Evita que um deles fique maior ao abrir a descrição */
  }

  .tarefas {
    overflow-y: auto;
    width: 100%;
    border-radius: 10px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    padding: 0.7rem;
    height: 100%;
    max-height: 100%;
    max-width: 100%;
    overflow-x: hidden;
    background: #b4b6b646;
  }

  .div-tarefa {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 100%;
    padding: 0.5rem 1rem;
    border-radius: 10px;
    background: white;
    box-shadow: 1.5px 1.5px 2px 2px rgba(0, 0, 0, 0.248);
    gap: 10px;
    max-width: 100%;
    flex-wrap: wrap;
    word-wrap: break-word;
  }

  .task-editTask {
    display: flex;
    justify-content: space-between;
    align-items: top;
    padding: 0;
    word-break: break-word;
  }

  .editTask {
    color: rgb(37, 37, 11);
    background: none;
    border: none;
    font-size: 22px;
    cursor: pointer;
    width: fit-content;
    height: fit-content;
    padding: 0.3rem;
    border-radius: 5px;
  }

  .editTask:hover {
    background: gainsboro;
    width: fit-content;
    height: fit-content;
  }

  .description {
    width: 100% !important;
    height: fit-content !important;
  }

  .div-tarefa:hover {
    transform: scale(1.01);
  }

  .deadline-btnsTasks {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
  }

  .div-tarefa h3 {
    font-size: 1.4rem;
    font-weight: 600;
    word-wrap: break-word;
  }

  .detalhes {
    /* width: fit-content; */
    /* max-width: 400px; */
    border-radius: 10px;
    box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.268);
  }

  .detalhes {
    background: #e3e7e859;
    /* background: #c4d0eb; */
  }

  .detalhes div {
    display: flex;
    max-width: 100%;
  }

  .detalhes h3 {
    margin: auto 10px;
    text-transform: uppercase;
    font-size: 24px;
    letter-spacing: 2px;
    font-weight: bold;
  }

  .detalhes p {
    margin: 20px 10px;
    font-size: 20px;
    font-weight: 400;
  }

  .toStart,
  .inProgress,
  .completed {
    display: flex;
    align-items: center;
    gap: 5px;
  }

  h4 {
    font-size: 24px;
    font-weight: 400;
  }

  .blue {
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: blue;
  }

  .orange {
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: orange;
  }

  .green {
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: green;
  }

  .btnsTarefa {
    display: flex;
    gap: 5px;
    width: fit-content;
  }

  .btnsTarefa div:hover {
    filter: brightness(120%);
  }

  .btnsTarefa div {
    width: 15px;
    height: 15px;
    border-radius: 50%;
    cursor: pointer;
  }
  .comecar {
    background: rgb(255, 166, 1);
  }
  .completar {
    background: rgba(1, 109, 1, 0.838);
  }
  .excluir {
    background: rgba(171, 1, 1, 0.833);
  }
</style>
